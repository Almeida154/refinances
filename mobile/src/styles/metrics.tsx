import { StatusBar } from 'react-native';

export default {
  default: {
    boundaries: 32,
    padding: 20,
    statusBarHeight: StatusBar.currentHeight,
  },
  inputText: {
    height: 40,
    radius: 10,
  },
};
